extends ../layout

block content
    .container
        h1 Collection
        .input-group
            input(type='url')
            .input-group-append
                a.btn.btn-primary
                    i.fas.fa-plus.text-white
        each bookmark in bookmarks
            h2= bookmark.title
            a(href=bookmark.editUrl title='Edit').mr-1.text-secondary
                i.fas.fa-edit
            a(href=bookmark.url)= bookmark.url
            p= bookmark.description