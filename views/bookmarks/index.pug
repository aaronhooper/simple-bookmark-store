extends ../layout

block content
    .container
        h1 Collection

        each bookmark in bookmarks
            h2= bookmark.title
            a(href=bookmark.url)= bookmark.url
            p= bookmark.description